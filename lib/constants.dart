import 'package:flutter/material.dart';

// i18n
const Locale defaultLanguage = Locale('en');

// Color constants
const appBarColor = Color(0xFF0D47A1);
const beenColor = Colors.green;
const wantColor = Color(0xFFBA68C8);
const favColor = Colors.orange;
