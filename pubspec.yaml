name: world_tracker
description: world tracker

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # firebase
  firebase_core: ^1.10.0
  cloud_firestore: ^3.1.0
  # State Management, Route and Utils
  get: ^4.3.8
  get_storage: ^2.0.3
  google_fonts: ^2.1.0
  flutter_svg: ^0.23.0+1
  # World map
  world_map:
    path: ./packages/world_map

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
